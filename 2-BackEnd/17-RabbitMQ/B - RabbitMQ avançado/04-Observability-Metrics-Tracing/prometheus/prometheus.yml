global:
  scrape_interval: 10s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: rabbitmq_observability_app
    static_configs:
      - targets: ["host.docker.internal:3074"]
