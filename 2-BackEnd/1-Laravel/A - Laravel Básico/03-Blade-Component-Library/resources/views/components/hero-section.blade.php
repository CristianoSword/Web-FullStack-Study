<section class="hero-section">
    <div class="hero-copy">
        <span class="section-kicker">{{ $tagline }}</span>
        <h1>{{ $title }}</h1>
        <p>{{ $copy }}</p>

        <div class="hero-actions">
            <a href="{{ $primaryHref }}" class="button button-primary">{{ $primaryLabel }}</a>
            <a href="{{ $secondaryHref }}" class="button button-secondary">{{ $secondaryLabel }}</a>
        </div>
    </div>

    <aside class="hero-panel">
        <p class="hero-panel-title">Base ideal para estudo</p>
        <ul>
            <li>Componentes Blade com responsabilidades claras</li>
            <li>Conteudo centralizado em configuracao</li>
            <li>Estrutura preparada para captacao de leads</li>
        </ul>
    </aside>
</section>
