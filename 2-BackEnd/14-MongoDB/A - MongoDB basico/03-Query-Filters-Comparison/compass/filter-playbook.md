# Filter Playbook

1. Conecte em `mongodb://localhost:27019/sales_analytics`.
2. Abra a collection `orders`.
3. Rode os comandos de `queries/comparison.mongodb.js`.
4. Compare os resultados dos cenarios com o dataset seedado.
5. Use `queries/verification.mongodb.js` para conferir o estado geral.
