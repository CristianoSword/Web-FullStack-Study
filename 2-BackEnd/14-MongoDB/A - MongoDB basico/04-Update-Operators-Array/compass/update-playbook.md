# Update Playbook

1. Conecte em `mongodb://localhost:27020/project_tracker`.
2. Abra a collection `projects`.
3. Rode `queries/update-operators.mongodb.js`.
4. Compare os arrays `labels`, `members` e `tasks.checkpoints`.
5. Finalize conferindo `queries/verification.mongodb.js`.
