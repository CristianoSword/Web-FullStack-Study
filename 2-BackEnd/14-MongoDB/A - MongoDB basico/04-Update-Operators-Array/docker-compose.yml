services:
  mongodb:
    image: mongo:8.0
    container_name: mongodb-update-operators
    restart: unless-stopped
    ports:
      - "27020:27017"
    volumes:
      - mongo_update_operators_data:/data/db
      - ./seed:/docker-entrypoint-initdb.d:ro

volumes:
  mongo_update_operators_data:
