services:
  mongodb:
    image: mongo:8.0
    container_name: mongodb-text-geolocation
    restart: unless-stopped
    ports:
      - "27023:27017"
    volumes:
      - mongo_text_geolocation_data:/data/db
      - ./seed:/docker-entrypoint-initdb.d:ro

volumes:
  mongo_text_geolocation_data:
