{
  "type": "object",
  "required": ["eventId", "storeId", "amount", "occurredAt"],
  "properties": {
    "eventId": { "type": "string" },
    "storeId": { "type": "string" },
    "amount": { "type": "number" },
    "occurredAt": { "type": "string" }
  }
}
