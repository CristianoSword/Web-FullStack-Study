{
  "type": "object",
  "required": ["idempotencyKey", "messageId", "status", "processedAt"],
  "properties": {
    "idempotencyKey": { "type": "string" },
    "messageId": { "type": "string" },
    "status": { "type": "string", "enum": ["PROCESSED", "DUPLICATE"] },
    "processedAt": { "type": "string" }
  }
}
