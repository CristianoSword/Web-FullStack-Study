# CLI Playbook

1. Execute `scripts/run-broker.ps1`.
2. Execute `scripts/bootstrap-topic.ps1`.
3. Rode `scripts/consume-stream.ps1` em um terminal.
4. Rode `scripts/produce-sample.ps1` em outro terminal.
5. Use `scripts/inspect-topic.ps1` para conferir particoes e replicas.
