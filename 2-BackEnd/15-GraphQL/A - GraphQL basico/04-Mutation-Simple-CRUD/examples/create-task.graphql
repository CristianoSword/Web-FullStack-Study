mutation CreateTask {
  createTask(
    input: {
      title: "Publish GraphQL CRUD lab"
      description: "Finalize documentation and validation."
    }
  ) {
    id
    title
    description
    status
  }
}
