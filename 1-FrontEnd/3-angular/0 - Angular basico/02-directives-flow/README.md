# 02 - Diretivas e Controle de Fluxo

Este projeto apresenta o novo sistema de controle de fluxo do Angular 17/18.

## Conceitos Aplicados
- `@if` / `@else`
- `@for` / `@empty`
- `track` property no loop for
- Event binding para alternar visibilidade

## Como rodar
1. `npm install`
2. `npm start`
