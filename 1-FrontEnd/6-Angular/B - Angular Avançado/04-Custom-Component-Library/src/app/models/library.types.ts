export type AuraButtonVariant = 'primary' | 'secondary' | 'outline' | 'danger';

export type AuraBadgeVariant = 'success' | 'warning' | 'info' | 'danger';

export interface AuraCardConfig {
  title?: string;
  subtitle?: string;
  hoverable?: boolean;
  clickable?: boolean;
}
