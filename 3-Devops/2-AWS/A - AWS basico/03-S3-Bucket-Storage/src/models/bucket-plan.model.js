export const createBucketPlan = ({
  bucketName,
  region,
  createBucketCommand,
  uploadCommands,
  verificationCommands
}) => ({
  bucketName,
  region,
  createBucketCommand,
  uploadCommands,
  verificationCommands
});
