export const createIamPlan = ({
  userName,
  groupName,
  managedPolicyArn,
  commands,
  verificationCommands
}) => ({
  userName,
  groupName,
  managedPolicyArn,
  commands,
  verificationCommands
});
