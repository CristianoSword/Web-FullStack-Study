export const createProfileSummary = ({
  profileName,
  region,
  outputFormat,
  configTemplatePath,
  credentialsTemplatePath
}) => ({
  profileName,
  region,
  outputFormat,
  configTemplatePath,
  credentialsTemplatePath
});
