export const createRdsPlan = ({
  instanceIdentifier,
  engine,
  instanceClass,
  allocatedStorageGb,
  createCommand,
  verificationCommands
}) => ({
  instanceIdentifier,
  engine,
  instanceClass,
  allocatedStorageGb,
  createCommand,
  verificationCommands
});
