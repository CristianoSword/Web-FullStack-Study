export const createLogPlan = ({
  appName,
  source,
  tailCommand,
  sourceCommand,
  dynoCommand,
  verificationCommands
}) => ({
  appName,
  source,
  tailCommand,
  sourceCommand,
  dynoCommand,
  verificationCommands
});
