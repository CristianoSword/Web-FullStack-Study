export const createDatabaseUrlReport = ({
  originalUrl,
  scheme,
  username,
  host,
  port,
  database,
  sslmode
}) => ({
  originalUrl,
  scheme,
  username,
  host,
  port,
  database,
  sslmode
});
