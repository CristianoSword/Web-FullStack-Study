export const createCommandPlan = ({
  primaryLoginCommand,
  interactiveFallbackCommand,
  verificationCommands,
  notes
}) => ({
  primaryLoginCommand,
  interactiveFallbackCommand,
  verificationCommands,
  notes
});
