apiVersion: v1
kind: Service
metadata:
  name: 02-pod-deployment-yaml-service
spec:
  type: NodePort
  selector:
    app: 02-pod-deployment-yaml
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
