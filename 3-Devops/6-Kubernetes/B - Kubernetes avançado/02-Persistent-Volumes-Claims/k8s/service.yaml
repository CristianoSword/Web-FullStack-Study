apiVersion: v1
kind: Service
metadata:
  name: 02-persistent-volumes-claims-service
spec:
  type: NodePort
  selector:
    app: 02-persistent-volumes-claims
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
