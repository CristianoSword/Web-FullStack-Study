apiVersion: apps/v1
kind: Deployment
metadata:
  name: 01-deployment-rolling-updates
  labels:
    app: 01-deployment-rolling-updates
spec:
  replicas: 2
  selector:
    matchLabels:
      app: 01-deployment-rolling-updates
  template:
    metadata:
      labels:
        app: 01-deployment-rolling-updates
    spec:
      containers:
      - name: web
        image: nginx:alpine
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "500m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
