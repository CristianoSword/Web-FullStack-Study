apiVersion: v1
kind: Service
metadata:
  name: 01-deployment-rolling-updates-service
spec:
  type: NodePort
  selector:
    app: 01-deployment-rolling-updates
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
