apiVersion: v1
kind: Service
metadata:
  name: 03-horizontal-pod-autoscaler-service
spec:
  type: NodePort
  selector:
    app: 03-horizontal-pod-autoscaler
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
