apiVersion: apps/v1
kind: Deployment
metadata:
  name: 03-horizontal-pod-autoscaler
  labels:
    app: 03-horizontal-pod-autoscaler
spec:
  replicas: 2
  selector:
    matchLabels:
      app: 03-horizontal-pod-autoscaler
  template:
    metadata:
      labels:
        app: 03-horizontal-pod-autoscaler
    spec:
      containers:
      - name: web
        image: nginx:alpine
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "500m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
