apiVersion: apps/v1
kind: Deployment
metadata:
  name: 04-ingress-controller-routing
  labels:
    app: 04-ingress-controller-routing
spec:
  replicas: 2
  selector:
    matchLabels:
      app: 04-ingress-controller-routing
  template:
    metadata:
      labels:
        app: 04-ingress-controller-routing
    spec:
      containers:
      - name: web
        image: nginx:alpine
        ports:
        - containerPort: 80
