{
  "name": "jest-config-scripts-lab",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --config jest.config.cjs",
    "test:watch": "jest --config jest.config.cjs --watch",
    "test:coverage": "jest --config jest.config.cjs --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
